[green_agent]
agentbeats_id = "019bc0c3-0f75-7c32-8f9b-a9f0087ca3ea"
env = { 
  NEO4J_URI = "${NEO4J_URI}",
  NEO4J_USERNAME = "${NEO4J_USERNAME}",
  NEO4J_PASSWORD = "${NEO4J_PASSWORD}",
  CHROMA_HOST = "chromadb",
  CHROMA_PORT = "8000",
  OPENROUTER_API_KEY = "${OPENROUTER_API_KEY}",
  PAPERS_URL = "https://github.com/arkhai-io/agentic-rag-green/releases/download/v1.0.0/papers.zip",
  QA_PAIRS_URL = "https://github.com/arkhai-io/agentic-rag-green/releases/download/v1.0.0/grounded_queries.zip"
}

[[participants]]
agentbeats_id = ""  # Purple agent submitters fill this
name = "rag_agent"
env = {}

[config]
# Assessment configuration - purple agents customize these values
domain = "female_longevity"
agent_name = ""           # Purple agent fills with their registered agent name
project_name = ""         # Purple agent fills with their project name
indexing_pipeline = ""    # Purple agent fills (e.g., "pdf_indexer")
retrieval_pipeline = ""   # Purple agent fills (e.g., "retriever")
